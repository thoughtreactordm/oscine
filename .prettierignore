# Generated by unplugin-auto-import / unplugin-vue-components on every dev run.
# Formatting them produces a diff that the next `npm run dev` throws away.
src/renderer/auto-imports.d.ts
src/renderer/components.d.ts

# Kleron's board, wiki and workstream state are tool-owned. The design doc is
# the project's design authority (see CLAUDE.md) and its hand-set wrapping is
# part of how it reads — a formatter has no business reflowing either.
.kleron/

# Prose is hand-wrapped throughout this repo. Prettier reflows markdown
# paragraphs to printWidth, which would rewrite every doc for no gain.
*.md

node_modules/
out/
dist/
release/
coverage/
package-lock.json
